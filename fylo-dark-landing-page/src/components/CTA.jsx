export default function CTA({ text }) {
  return (
    <button className="font-semibold font-raleway border-none outline-none py-3 w-full bg-gradient-to-r from-cyang to-blueg hover:from-blueg hover:to-blueg rounded-full">
      {text}
    </button>
  );
}
